// This file was auto-generated by Fern from our API Definition.

import type * as VitableConnect from "../index.js";

export interface DeductionDetail {
    /** Deduction category (reserved for future use) */
    deduction_category: string | null;
    /** Employee deduction amount in cents */
    deduction_amount_in_cents: number;
    /**
     * Tax classification: Pre-tax, Post-tax, or Unknown
     *
     * * `Unknown` - Unknown
     * * `Pre-tax` - Pre Tax
     * * `Post-tax` - Post Tax
     */
    tax_classification: VitableConnect.TaxClassification;
    /**
     * Deduction frequency
     *
     * * `weekly` - Weekly
     * * `bi_weekly` - Bi Weekly
     * * `semi_monthly` - Semi Monthly
     * * `monthly` - Monthly
     */
    frequency: VitableConnect.DeductionFrequency;
    /** Name of the benefit plan */
    benefit_name: string;
    /** Period start date (YYYY-MM-DD) */
    period_start_date: string;
    /** Period end date (YYYY-MM-DD) */
    period_end_date: string;
}
