// This file was auto-generated by Fern from our API Definition.

import { AuthClient } from "./api/resources/auth/client/Client.js";
import { EmployeesClient } from "./api/resources/employees/client/Client.js";
import { EmployersClient } from "./api/resources/employers/client/Client.js";
import { EnrollmentsClient } from "./api/resources/enrollments/client/Client.js";
import { GroupsClient } from "./api/resources/groups/client/Client.js";
import { MembersClient } from "./api/resources/members/client/Client.js";
import { OrganizationsClient } from "./api/resources/organizations/client/Client.js";
import { PlansClient } from "./api/resources/plans/client/Client.js";
import { WebhookEventsClient } from "./api/resources/webhookEvents/client/Client.js";
import type { BaseClientOptions, BaseRequestOptions } from "./BaseClient.js";
import { type NormalizedClientOptionsWithAuth, normalizeClientOptionsWithAuth } from "./BaseClient.js";
import * as core from "./core/index.js";

export declare namespace VitableConnectClient {
    export type Options = BaseClientOptions;

    export interface RequestOptions extends BaseRequestOptions {}
}

export class VitableConnectClient {
    protected readonly _options: NormalizedClientOptionsWithAuth<VitableConnectClient.Options>;
    protected _auth: AuthClient | undefined;
    protected _employees: EmployeesClient | undefined;
    protected _employers: EmployersClient | undefined;
    protected _enrollments: EnrollmentsClient | undefined;
    protected _groups: GroupsClient | undefined;
    protected _members: MembersClient | undefined;
    protected _organizations: OrganizationsClient | undefined;
    protected _plans: PlansClient | undefined;
    protected _webhookEvents: WebhookEventsClient | undefined;

    constructor(options: VitableConnectClient.Options = {}) {
        this._options = normalizeClientOptionsWithAuth(options);
    }

    public get auth(): AuthClient {
        return (this._auth ??= new AuthClient(this._options));
    }

    public get employees(): EmployeesClient {
        return (this._employees ??= new EmployeesClient(this._options));
    }

    public get employers(): EmployersClient {
        return (this._employers ??= new EmployersClient(this._options));
    }

    public get enrollments(): EnrollmentsClient {
        return (this._enrollments ??= new EnrollmentsClient(this._options));
    }

    public get groups(): GroupsClient {
        return (this._groups ??= new GroupsClient(this._options));
    }

    public get members(): MembersClient {
        return (this._members ??= new MembersClient(this._options));
    }

    public get organizations(): OrganizationsClient {
        return (this._organizations ??= new OrganizationsClient(this._options));
    }

    public get plans(): PlansClient {
        return (this._plans ??= new PlansClient(this._options));
    }

    public get webhookEvents(): WebhookEventsClient {
        return (this._webhookEvents ??= new WebhookEventsClient(this._options));
    }

    /**
     * Make a passthrough request using the SDK's configured auth, retry, logging, etc.
     * This is useful for making requests to endpoints not yet supported in the SDK.
     * The input can be a URL string, URL object, or Request object. Relative paths are resolved against the configured base URL.
     *
     * @param {Request | string | URL} input - The URL, path, or Request object.
     * @param {RequestInit} init - Standard fetch RequestInit options.
     * @param {core.PassthroughRequest.RequestOptions} requestOptions - Per-request overrides (timeout, retries, headers, abort signal).
     * @returns {Promise<Response>} A standard Response object.
     */
    public async fetch(
        input: Request | string | URL,
        init?: RequestInit,
        requestOptions?: core.PassthroughRequest.RequestOptions,
    ): Promise<Response> {
        return core.makePassthroughRequest(
            input,
            init,
            {
                baseUrl: this._options.baseUrl ?? this._options.environment,
                headers: this._options.headers,
                timeoutInSeconds: this._options.timeoutInSeconds,
                maxRetries: this._options.maxRetries,
                fetch: this._options.fetch,
                logging: this._options.logging,
                getAuthHeaders: async () => (await this._options.authProvider.getAuthRequest()).headers,
            },
            requestOptions,
        );
    }
}
